import { DropdownOptionDirective } from './dropdown-option.directive';

describe('DropdownOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownOptionDirective();
    expect(directive).toBeTruthy();
  });
});
