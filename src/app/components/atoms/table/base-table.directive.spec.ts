import { BaseTableDirective } from './base-table.directive';

describe('BaseTableDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseTableDirective();
    expect(directive).toBeTruthy();
  });
});
