import { CellFlushDirective } from './cell-flush.directive';

describe('CellFlushDirective', () => {
  it('should create an instance', () => {
    const directive = new CellFlushDirective();
    expect(directive).toBeTruthy();
  });
});
