import { MenuGroupDirective } from './menu-group.directive';

describe('MenuGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuGroupDirective();
    expect(directive).toBeTruthy();
  });
});
