import { SectionCardDirective } from './section-card.directive';

describe('SectionCardDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionCardDirective();
    expect(directive).toBeTruthy();
  });
});
