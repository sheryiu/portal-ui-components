import { FormFieldIconDirective } from './form-field-icon.directive';

describe('FormFieldIconDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldIconDirective();
    expect(directive).toBeTruthy();
  });
});
