import { FormFieldTitleDirective } from './form-field-title.directive';

describe('FormFieldTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldTitleDirective();
    expect(directive).toBeTruthy();
  });
});
