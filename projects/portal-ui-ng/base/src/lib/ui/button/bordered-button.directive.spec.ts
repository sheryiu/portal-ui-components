import { BorderedButtonDirective } from './bordered-button.directive';

describe('BorderedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
