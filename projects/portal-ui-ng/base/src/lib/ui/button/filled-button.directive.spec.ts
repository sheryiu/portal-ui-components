import { FilledButtonDirective } from './filled-button.directive';

describe('FilledButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FilledButtonDirective();
    expect(directive).toBeTruthy();
  });
});
