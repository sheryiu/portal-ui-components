import { BaseButtonDirective } from './base-button.directive';

describe('BaseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
