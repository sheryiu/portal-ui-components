import { ContentVisibilityDetectorDirective } from './content-visibility-detector.directive';

describe('ContentVisibilityDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentVisibilityDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
