import { LayoutControlDirective } from './layout-control.directive';

describe('LayoutControlDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutControlDirective();
    expect(directive).toBeTruthy();
  });
});
