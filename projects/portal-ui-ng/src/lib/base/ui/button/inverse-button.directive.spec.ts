import { InverseButtonDirective } from './inverse-button.directive';

describe('InverseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new InverseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
