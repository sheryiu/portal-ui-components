import { ScrollspyTriggerDirective } from './scrollspy-trigger.directive';

describe('ScrollspyTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollspyTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
